- hosts: all
  user: root
  tasks:

  - package: name=dracut-fips state=present

  - shell: grep -w aes /proc/cpuinfo
    failed_when: no
    changed_when: no
    register: is_aesni

  - package: name=dracut-fips-aesni state=present
    when: not is_aesni.rc



